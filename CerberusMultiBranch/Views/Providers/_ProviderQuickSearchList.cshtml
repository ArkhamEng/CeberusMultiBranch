@model IEnumerable<ProviderViewModel>

@using CerberusMultiBranch.Support
@using CerberusMultiBranch.Models.ViewModels.Catalog



<table id="tbProviderQuickSearch" class="table dt-responsive  table-bordered small hidden" style="width:100%">
    <thead>
        <tr>
            <th class="col-md-4">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th class="col-md-2">
                @Html.DisplayNameFor(model => model.FTR)
            </th>
            <th class="col-md-2">
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th class="col-md-2">
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th/>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-uppercase">
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td class="text-uppercase">
                    @Html.DisplayFor(modelItem => item.FTR)
                </td>
                <td class="text-lowercase">
                   @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td class="text-uppercase">
                    @Html.DisplayFor(modelItem => item.Phone)
                </td>
                <td >
                    <button type="button" class="btn btn-primary btn-xs" onclick="ProviderSelected('@item.ProviderId','@item.Name','@item.FTR')">
                        <span class="fa fa-check"></span>
                        Seleccionar
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    $(document).ready(function ()
    {
        $("#tbProviderQuickSearch").removeClass("hidden");
        Paginate("#tbProviderQuickSearch", 8, true, "#FClientName", false);
    });

    var recordSelected;

    function ProviderSelected(id, name, ftr)
    {
        HideModal(function () { recordSelected(id, name, ftr); }, true);
    }

   
</script>