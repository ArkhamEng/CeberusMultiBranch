
<div id="ModalConfirm" class="modal" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content panel panel-danger ">
            <div class="modal-header panel-heading">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 id="cnfHeader" class="modal-title">
                    Eliminar registro?
                </h4>
            </div>
            <div class="panel-body modal-body">
                <h4 id="cnfText">
                    Una vez realizada esta acción no hay vuelta atras, 
                    ¿Estas seguro de eliminar este registro?
                </h4>
            </div>
            <div class="modal-footer">
                <div class="btn-group">
                    <button type="button" class="btn btn-primary" id="btnConfirm"  >
                        Aceptar
                        <span class="fa fa-check"></span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function ()
    { }).keydown(function (e)
    {
        if ($("#ModalConfirm").hasClass('in'))
        {
            console.log("keydown on confirm accepted");

            e.preventDefault();
            e.stopImmediatePropagation();

            if (e.keyCode == 13)
            {
                $("#btnConfirm").click();
            }
            else if (e.keyCode == 27)
            {
                $("#ModalConfirm").modal("hide");
            }
        }
    });
</script>